APP_PASSWORD=change-me
# Optional alternative to APP_PASSWORD. Generate with:
# node -e "crypto=require('crypto');console.log(crypto.createHash('sha256').update('your-password').digest('hex'))"
APP_PASSWORD_SHA256=
CRON_SECRET=change-this-too
# Optional. Used for Semantic Scholar calls if you have one.
SEMANTIC_SCHOLAR_API_KEY=
